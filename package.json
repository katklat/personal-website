{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Professional website",
  "main": "index.html",
  "scripts": {
    "start": " npm run sass && npm-run-all --parallel sass:watch serve",
    "sass": "node-sass -w --output-style --source-map true compressed scss/styles.scss docs/styles.css  ",
    "sass:watch": "npm run sass -- -w",
    "serve": "serve docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.1",
    "npm-run-all": "^4.1.5",
    "serve": "^11.3.0"
  }
}
